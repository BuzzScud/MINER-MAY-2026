DATABASE_URL=postgresql://localhost:5433/algonov26
JWT_SECRET=change-me-to-a-random-32-char-string
PORT=4000
# If set: creates admin user when missing; when admin exists, updates admin password on every server start (use to reset admin login)
ADMIN_INITIAL_PASSWORD=change-me-on-first-run
# Production: comma-separated list of allowed CORS origins (e.g. https://yourdomain.com,https://www.yourdomain.com)
ALLOWED_ORIGINS=
